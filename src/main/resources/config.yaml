
connection: jdbc:oracle:thin:tiger/scott@localhost:1521:productDB
directory: /data/storage/dump
entities:
   EXPORT_TAB_MEMO:
      skip: true #true or flase (optional)
      query: select * from table
      description: prova descrizione
   PRODUCT:
      query: select * from product